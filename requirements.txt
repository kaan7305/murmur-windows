# Everything a source checkout needs. setup.bat installs exactly this list and
# nothing else, so a package missing from here is a fresh clone that does not
# start - which is how PySide6 came to be absent from it for four releases.
faster-whisper>=1.2
PySide6>=6.6
sounddevice>=0.5
numpy>=2.0
pynput>=1.8

# Not listed on purpose: ctranslate2, huggingface_hub, tokenizers and
# onnxruntime all arrive with faster-whisper. Pinning them here would mean
# holding a second opinion about versions faster-whisper already has one on.
